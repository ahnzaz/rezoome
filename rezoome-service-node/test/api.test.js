/**
 * Unit test suite for API service. <br />
 *
 * @since 1.0.0
 */
 
 const localOption = {
     hostname : 'localhost',
     
 }
 describe('User API suite', function(){
     it('Get all user', function(done){
         
     });
     
     it('Put new user', function(done){
         
     });
 })