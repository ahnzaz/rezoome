var mongoose = require("mongoose");
var propertyLoader = ;

var dbHost = dataLoader.getConfig("db_host");
var dbPort = dataLoader.getConfig("db_port);
var dbUserId = dataLoader.getConfig("db_userId");